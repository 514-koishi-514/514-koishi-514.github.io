---
title: "Welcome"
format: html
---

# 你好，我是 514-koishi-514

这里会成为一个小而灵活的个人主页：可以展示项目、整理课程实验，也可以把日常写作放在 Markdown 或 QMD 里。

## 现在可以做什么

- 点击左侧示例文件切换内容。
- 上传或拖入 `.md`、`.markdown`、`.qmd`、`.txt` 文件。
- 使用代码块、表格、引用和数学公式，例如 $E = mc^2$。

## 示例代码

```python
def hello(name: str) -> str:
    return f"hello, {name}"

print(hello("GitHub Pages"))
```

> 页面是纯静态的，直接托管在 GitHub Pages 上，不需要服务器。
